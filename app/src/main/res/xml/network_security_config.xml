<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 🔒 SECURITY: Block all clear-text traffic by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    
    <!-- 🏠 LOCAL DEVELOPMENT: Allow clear-text only for local server -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    
    <!-- 🔐 PRODUCTION: Enforce HTTPS with certificate pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">yourdomain.com</domain>
        <pin-set expiration="2025-12-31">
            <!-- Replace with your actual certificate SHA-256 hash -->
            <pin digest="SHA-256">REPLACE_WITH_YOUR_CERTIFICATE_HASH</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>
    
    <!-- 🚫 SECURITY: Block known malicious domains -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">malicious-domain.com</domain>
    </domain-config>
</network-security-config>
